[project]
name = "data-charts-api"
version = "0.1.0"
description = "api for data charts project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.30.0",
    "fastapi[standard]>=0.141.1",
    "python-dotenv>=1.0.1",
    "uvicorn>=0.52.1",
]

[dependency-groups]
dev = ["pytest>=9.1.1", "requests>=2.34.2", "ruff>=0.16.2"]
